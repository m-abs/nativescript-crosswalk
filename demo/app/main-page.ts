import view = require("ui/core/view");
import {Page} from "ui/page";
import * as observable from 'data/observable';
import * as pages from 'ui/page';

export function pageLoaded(arg: any) {
  const page: Page = arg.object;
}
